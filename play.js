/* 
let uName = 'max';
let age = 29;
let hasHobbies = true;

const summariseUser = (userName, userAge, userHobbies) => {
  return (
    'Name is ' +
    userName +
    ', age is ' +
    userAge +
    ', has hobbies: ' +
    userHobbies
  );
};
console.log(summariseUser(uName, age, hasHobbies));
*/

setTimeout(() => {
  console.log('timer is done!!!');
}, 2000);
